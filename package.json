{
  "name": "stock-metrics-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "Daily sector/industry-metrics data pipeline for stock-analyzer",
  "scripts": {
    "generate": "node scripts/generateSectorMetrics.js"
  },
  "engines": {
    "node": ">=18"
  }
}
